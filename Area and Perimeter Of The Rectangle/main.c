//efejzo20
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    int area,perimeter;
    area=a*b;
    perimeter=2*(a+b);
    printf("%d %d",area,perimeter);

    return 0;
}
